name: Build and deploy

on:
  push:
    branches:
      - "*"
      - "!master"

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres

    steps:
      - uses: actions/checkout@v2
      - name: Install and test
        run: |
          npm install
          npm run test
